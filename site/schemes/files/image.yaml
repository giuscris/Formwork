title: Image

extend: files.file

layout:
    type: sections
    sections:
        metadata:
            label: '{{file.metadata}}'
            fields: [alt]

fields:
    alt:
        type: text
        label: '{{file.metadata.alternativeText}}'
